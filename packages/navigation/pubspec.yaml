name: navigation
description: Navigation contract and route constants
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.9.2
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.0.0
  get_it: ^9.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
