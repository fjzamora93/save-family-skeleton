name: sf_shared
description: Shared error model, AsyncValue UI helpers, and testing utilities
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.9.2
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.3

  localizations:
    path: ../localizations

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
